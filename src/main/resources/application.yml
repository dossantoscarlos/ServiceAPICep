spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/table_cep
    username: postgres
    password: carlos
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update

springdoc:
  api-docs:
    path: /api-docs
    swagger-ui:
        path: /swagger-ui.html
        enabled: true